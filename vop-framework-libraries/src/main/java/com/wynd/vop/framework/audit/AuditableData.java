package com.wynd.vop.framework.audit;

/**
 * Interface used as a marker for request and response data that are to be audited, at REST end-points and partner client library calls
 */
public interface AuditableData {
	// marker interface only, no methods declared
}